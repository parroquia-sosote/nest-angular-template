meta {
  name: SignIn
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/signin
  body: json
  auth: none
}

body:json {
  {
    "email": "example@example.com",
    "password": "password"
  }
}
